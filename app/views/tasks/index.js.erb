var taskTable = document.getElementById('tasks-table')

// Removes contents from tasks table within the DOM
taskTable.innerHTML = "";

// Displays the filtered by tasks within the tasks table
taskTable.insertAdjacentHTML("afterbegin",
  "<%= escape_javascript render "table", task: @tasks %>"
);
